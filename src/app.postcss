@tailwind base;
@tailwind components;
@tailwind utilities;

.text-japanese {
  @apply text-base;
  /* 文字間隔 5% */
  @apply tracking-wider;
  /* 行間隔 1.625 */
  @apply leading-relaxed;
}

.background-color {
  @apply dark:bg-dark dark:text-gray-50;
}
