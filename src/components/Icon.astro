---
import { Icon as AstroIcon } from 'astro-icon';

export interface Props {
  pack?: string;
  name: string;
  size: keyof typeof sizes;
}

const sizes = {
  md: 1.5,
} as const;

const { pack, name, size } = Astro.props;
---

<AstroIcon {pack} {name} style={`--size: ${sizes[size]}rem;`} />

<style>
  [astro-icon] {
    height: var(--size);
    width: var(--size);
  }
</style>
